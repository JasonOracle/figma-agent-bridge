# USER_GUIDE — 使用教程

写给第一次使用的你。不需要理解任何内部架构——安装完成后（见 [SETUP.md](SETUP.md)），**你只需要用一句话描述想设计什么**。

## 一句话能设计什么

只要说清楚：**产品类型 + 页面 + 想要的感觉**。行业、配色、字体、组件 Skill 会自己推导。

| 你说 | Skill 会自动得到 |
|---|---|
| "设计一个现代 AI 医疗健康 App 首页，要有健康评分、趋势图、健康建议和底部导航，整体高级简洁，适合 iPhone" | 医疗行业青绿色系、移动端 402×874 画布、评分卡/趋势图/建议卡/底部导航组件 |
| "设计一个河南高速智慧养护大屏" | 政务大屏暗色风格、3840×2160 画布、数据可视化组件 |
| "设计一个企业后台 Dashboard，简洁专业" | 企业中后台风格、桌面 1440×900 画布、表格/图表/表单组件 |

---

## 第一次运行全流程（以医疗 App 为例）

对 Agent 说：

> 设计一个现代 AI 医疗健康 App 首页

然后你只需要在两个确认点点一下，其余全自动：

### Step 1 — 设计定位（Brief）

Skill 自动推导风格路线（如"高级 SaaS 风"）、平台（手机 App）、行业主色，生成 `design-brief.json`，并展示给你看。

**你做**：看一眼是否符合预期 → 确认继续（或提出修改，如"换成暖色调"）。

### Step 2 — 设计系统（Design System）

由 Brief 自动生成完整设计系统：色板、字阶、间距、圆角、组件清单（哪些复用、哪些新建，各带理由），生成 `design-system-spec.json`。

**你做**：确认继续。之后无需再动手。

### Step 3 — Figma 页面自动生成

Skill 在你的 Figma 文件里新建页面并自动绘制（分批执行，每批都会回读校验）。你会看到 Figma 画布上逐步出现页面。

### Step 4 — 自动视觉质量审查

自动从五个维度打分（布局 / 色彩 / 一致性 / 商业感 / 易用性，各 10 分）：

- **平均 ≥8 分且无单项低于 7**：通过，继续导出
- **有问题**：自动定位问题 → 修复 → 复检，最多 3 轮；3 轮仍不达标会如实告诉你（不会硬凑分数）

最终得到 `critic-report.json`（分数 + 每条问题的证据与修复记录）。

### Step 5 — 交付导出

自动导出 PNG（@2x，给评审/汇报用）、SVG（矢量，给开发用）和 `export-manifest.json`（全部交付物的清单与映射：Figma 节点 ↔ 设计系统 ↔ 前端组件 ↔ CSS 变量）。

---

## 你会看到的文件

| 文件 | 是什么 | 给谁用 |
|---|---|---|
| `design-brief.json` | 设计定位（风格/行业/平台/约束） | 评审方向 |
| `design-system-spec.json` | 设计系统全量定义 | 设计/开发对接 |
| `build-plan.json` | Figma 构建计划（分批、操作数） | 了解生成过程 |
| Figma 页面 | 成品画布 | 设计稿本体 |
| `critic-report.json` | 五维评分 + 问题 + 修复记录 | 质量追溯 |
| `export-manifest.json` | 交付清单与三层映射 | 开发落地 |
| PNG / SVG | 截图与矢量 | 汇报 / 开发 |

## 常见问题

**Q：不装 Figma 插件会怎样？**
Skill 正常输出全部设计文档，并在最后明确告知"当前环境只有读取能力，需要安装 Figma Bridge 才能自动绘制"（或仅输出文档）。不会假装画过。

**Q：生成的页面在我 Figma 的哪里？**
当前打开的设计文件内新建页面/画框，放在画布空白区（不会覆盖你已有的内容）。

**Q：可以迭代修改吗？**
可以。直接说"把主色换成蓝色"、"评分卡再大气一点"，Skill 会只修改受影响的部分并复检。

**Q：审查没通过怎么办？**
报告里每条问题都有证据（节点实测数据）和建议。最多自动修 3 轮；仍不达标会给出"转人工"的明确结论，而不是硬过。

**Q：多长时间？**
环境就绪后首次运行约 3–5 分钟（含两次确认）；再次运行更快。

更多排查见 [SETUP.md](SETUP.md) 的故障排查表。
